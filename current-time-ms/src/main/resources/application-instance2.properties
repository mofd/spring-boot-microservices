info.name=CurrentTimeMs:Instance2

application.name=CurrentTimeMs

spring.application.name=CurrentTimeMs
server.port=9852

eureka.instance.metadata-map.instanceId=${spring.application.name}:${server.port}:${spring.application.instance_id:${random.value}}
eureka.instance.lease-expiration-duration-in-seconds=10
eureka.instance.lease-renewal-interval-in-seconds=5
eureka.client.service-url.defaultZone=http://localhost:8181/eureka/,http://localhost:8282/eureka/

date.format="dd.MM.yyyy HH:MM"